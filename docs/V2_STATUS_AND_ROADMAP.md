# AI 简历筛选工作台 V2 状态总览与路线图

## 1. 文档定位

本文档是 V2 / V2.1 / V2.1.1 / V2.1.2 的当前权威状态总览，用于回答三个问题：

- V2 已经做完什么。
- V2 明确还没做什么。
- 下一阶段建议做什么。

历史拆解文档仍保留用于追溯实现过程：

- [HR_RESUME_SCREENING_TOOL_V2_DESIGN.md](./HR_RESUME_SCREENING_TOOL_V2_DESIGN.md)：V2 产品设计与模块边界。
- [V2_TASK_BREAKDOWN.md](./V2_TASK_BREAKDOWN.md)：V2 Phase 7-16 历史任务拆解。
- [V2_1_TASK_BREAKDOWN.md](./V2_1_TASK_BREAKDOWN.md)：V2.1 Phase 18-20 历史任务拆解。
- [V2_1_1_TASK_BREAKDOWN.md](./V2_1_1_TASK_BREAKDOWN.md)：V2.1.1 / V2.1.2 历史任务拆解。
- [V2_ISSUE_FIX_PLAN.md](./V2_ISSUE_FIX_PLAN.md)：岗位候选人边界修复历史方案。
- [V2_ACCEPTANCE_GUIDE.md](./V2_ACCEPTANCE_GUIDE.md)：当前验收命令和回归路径。

## 2. 当前结论

V2 到 V2.1.2 已经形成“可试用招聘工作台”的主闭环：

```text
工作台总览
  -> 创建 / 管理岗位
  -> 上传简历
  -> 查看上传队列与失败重试
  -> 解析简历并生成候选人
  -> 自动评分与分项解释
  -> 候选人列表筛选与批量操作
  -> 候选人详情复核、备注、时间线
  -> 人才库沉淀与跨岗位搜索
  -> 岗位标准版本变更与历史候选人重评
  -> 重复候选人人工复核
  -> 推荐摘要 Markdown 生成、编辑、复制、下载
```

V2.1.x 继续保持边界：不做自动合并候选人，不做候选人主档案合并，不引入多岗位归属模型。

## 3. 已完成范围

### 3.1 V2 基础能力

- V2 基础模型、Schema、Repository 和迁移。
- 上传处理任务、匹配分项解释、候选人备注、时间线、标签和标签关联。
- 候选人岗位状态与人才库状态分离。
- 操作时间线追加写入，不覆盖历史。

### 3.2 工作台首页

- `/dashboard` 页面。
- `/api/dashboard`、summary、todos、recent activities。
- 开放岗位、候选人、高匹配、待沟通、解析失败、评分失败、重复待复核等统计。
- 今日待办、异常概览、最近活动。

### 3.3 上传队列与失败重试

- 岗位内简历上传。
- 上传任务列表。
- 单任务重试和批量重试失败项。
- 上传、解析、评分状态与失败原因展示。
- 重复风险统计展示。

### 3.4 候选人列表增强

- 按状态、推荐等级、分数区间、城市、工作年限、学历、技能、风险、低置信度、入库状态筛选。
- 批量状态更新。
- 批量加入人才库。
- 批量重新评分。
- 部分候选人不属于当前岗位时跳过或返回部分失败。

### 3.5 AI 匹配解释与原文复核

- 候选人匹配结果。
- 分项解释、结论、证据片段。
- 原简历预览。
- 字段来源定位和关键词高亮。
- 敏感联系方式不参与推荐摘要和自动匹配判断。

### 3.6 备注、时间线与人工操作追溯

- 候选人备注新增和列表。
- 上传、解析、评分、字段修正、状态变更、批量操作、人才库流转、重复复核等写入时间线。
- 岗位上下文校验，避免跨岗位误操作。

### 3.7 轻量人才库

- 人才库候选人列表。
- 跨岗位搜索。
- 技能、城市、学历、年限筛选。
- 候选人历史岗位。
- 最近匹配结果。
- 标签新增和移除。
- 从候选人详情加入或移出人才库。

### 3.8 岗位管理增强

- 岗位复制、暂停、重新开放、关闭。
- 岗位漏斗统计。
- JD 质量检查。
- `open` 和 `paused` 岗位可编辑。
- `closed` 岗位禁止编辑和重新开放，只能复制后修改。

### 3.9 V2.1 统一上传与重复识别

- 统一上传 API：`POST /api/resumes/upload`。
- 统一上传页：`/resumes/upload`。
- 上传前必须选择归属岗位。
- 岗位内上传继续保留并复用同一处理链路。
- 疑似重复候选人记录：手机号完全匹配、邮箱完全匹配、姓名 + 城市匹配。
- 第一版只提示，不自动合并。

### 3.10 岗位标准版本与重评闭环

- 岗位创建和复制时生成初始标准版本。
- JD、职责、必备条件、加分项、排除条件、评分维度变更时生成新版本。
- 非标准字段变更不生成新版本。
- 匹配结果关联 `job_standard_version_id`。
- 岗位详情展示当前标准版本和版本历史。
- 岗位级批量重新评分接口：`POST /api/jobs/{job_id}/candidates/rematch`。
- 重评结果关联最新岗位标准版本。

### 3.11 推荐摘要第一版

- 推荐摘要接口：`GET /api/jobs/{job_id}/candidates/{candidate_id}/match/recommendation-report`。
- 基于岗位、候选人、最新匹配和分项解释生成 Markdown。
- 候选人详情页可生成、编辑、复制和下载 `.md`。
- 第一版不保存历史，不导出 PDF / Word。

### 3.12 重复候选人人工复核

- 重复检查状态：`pending_review`、`ignored`、`confirmed_duplicate`。
- 候选人详情展示重复风险与复核操作。
- 工作台展示待复核重复风险待办。
- 上传队列展示待复核、已忽略、确认重复统计。
- 复核动作写入候选人时间线。
- 不合并候选人，不修改候选人主档案。

### 3.13 工程与验收

- 后端 pytest 覆盖 V2 主链路、岗位边界、统一上传、重复识别、标准版本、重评和重复复核。
- 后端 ruff 可执行。
- 前端 lint 使用非交互配置。
- 前端生产构建通过。
- API 手动测试脚本包含全量清理工具，默认预览，`--yes` 才执行删除。

## 4. 未完成但适合后续 V2.1.x 的事项

### 4.1 报告历史保存

当前状态：推荐摘要可生成、编辑、复制、下载，但不入库。

建议阶段：V2.1.3。

建议范围：

- 新增推荐报告历史表。
- 保存 Markdown 草稿。
- 支持历史报告列表。
- 支持打开历史报告继续编辑、复制、下载。
- 保存或更新报告时写入时间线。

### 4.2 文本粘贴录入简历

当前状态：只支持文件上传。

建议阶段：V2.1.4。

建议范围：

- 新增文本录入入口。
- 录入时选择岗位。
- 复用现有解析、评分、重复识别、时间线和上传任务状态链路。
- 明确文本录入生成的 `ResumeFile` 或等价记录如何保存。

### 4.3 ZIP 批量上传

当前状态：前端可多文件选择并逐个上传，但不支持 ZIP 解包。

建议阶段：V2.1.5 或后置。

建议范围：

- 上传 ZIP。
- 服务端解包。
- 逐个文件复用现有单文件处理链路。
- 每个文件保留独立任务状态和失败原因。

### 4.4 PDF / Word 推荐报告导出

当前状态：仅支持 Markdown 下载。

建议阶段：报告历史保存之后。

建议范围：

- 从已保存报告导出 PDF / Word。
- 导出前仍允许人工编辑。
- 导出内容继续遵守敏感信息和风险提示约束。

### 4.5 AI 匹配解释升级

当前状态：分项解释已可用，但主要基于当前匹配结果和候选人字段生成。

建议范围：

- 让 AI Provider 输出更严格的 JSON 分项解释。
- 对解释内容做结构化校验和兜底。
- 提升证据片段和维度评分质量。

### 4.6 上传处理异步化

当前状态：同步处理为主，已记录任务状态。

触发条件：真实批量上传等待时间明显影响使用。

建议范围：

- 引入后台队列。
- 保留现有任务状态表作为前端查询基础。
- 不改变上传任务和候选人生成的业务语义。

## 5. 明确不进入 V2 / V2.1.x 的事项

- 自动合并重复候选人。
- 候选人主档案合并。
- 多岗位同时归属同一份简历。
- 完整 CRM 式人才库运营。
- 复杂多角色权限体系。
- 完整多人协作审批流程。
- 面试日程管理。
- 邮件、短信、企业微信、飞书通知触达。
- ATS 系统对接。
- 招聘数据大屏。
- Offer 管理。
- 图片简历 OCR。

其中“候选人主档案合并”“重复候选人合并”“多岗位归属模型”统一归入 V4 人才库增强，不进入 V2.1.x。

## 6. 建议下一阶段

建议下一阶段进入：

```text
V2.1.3：推荐报告历史保存
```

原因：

- 已有推荐摘要生成、编辑、复制、下载能力，继续做历史保存是最小闭环。
- 不触碰上传解析主链路，风险低。
- 对真实 HR 使用价值直接：推荐材料可保存、回看和复用。
- 不进入 V4 的候选人主档案合并范围。

建议验收标准：

- HR 可以保存当前候选人的推荐摘要草稿。
- HR 可以查看该候选人在当前岗位下的历史报告。
- HR 可以打开历史报告继续编辑、复制和下载 Markdown。
- 保存和更新报告会写入候选人时间线。
- 岗位上下文校验仍然生效，不能跨岗位读取或写入报告。
- 自动化测试覆盖报告创建、列表、更新、时间线和岗位边界。

## 7. 当前验证命令

```powershell
cd backend
.\venv\Scripts\python.exe -m pytest tests
.\venv\Scripts\python.exe -m ruff check app tests

cd ..\frontend
npm run lint
npm run build
```

## 8. 文档归并结果

当前建议的文档职责如下：

- `HR_RESUME_SCREENING_TOOL_V2_DESIGN.md`：保留，作为产品设计和模块边界文档。
- `V2_STATUS_AND_ROADMAP.md`：新增，作为 V2 当前状态和后续路线图的权威总览。
- `V2_ACCEPTANCE_GUIDE.md`：保留，作为验收命令和手动回归入口。
- `V2_TASK_BREAKDOWN.md`：保留为历史归档，不再作为当前计划入口。
- `V2_1_TASK_BREAKDOWN.md`：保留为历史归档，不再作为当前计划入口。
- `V2_1_1_TASK_BREAKDOWN.md`：保留为历史归档，不再作为当前计划入口。
- `V2_ISSUE_FIX_PLAN.md`：保留为历史归档，岗位边界问题已修复。
